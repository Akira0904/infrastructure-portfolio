# Docker — Phase 3

This directory is reserved for a later comparison between:

1. Native systemd-based monitoring services
2. Containerized Prometheus/Grafana deployment using Docker Compose

The goal is to document operational trade-offs rather than include Docker only as a keyword.
