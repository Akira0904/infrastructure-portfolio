# Docker — Planned Extension

Potential portfolio use case: Prometheus, Grafana and Alertmanager via Docker Compose.

The goal is to compare containerized and native-service operations rather than add Docker only as a keyword.
